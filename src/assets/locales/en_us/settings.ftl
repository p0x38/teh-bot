settings-help = 
    Usage: `{ $prefix }settings { $category } <key`
    Type `{ $prefix }settings { $category } help` for a list of keys.

settings-updated =
    { $botnick } has updated the **{ $category }** setting **{ $key }** to `{ $value }`.